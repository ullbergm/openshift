apiVersion: v1
kind: ConfigMap
metadata:
  name: dump978-gatus-config
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |-
    endpoints:
      - name: dump978
        group: {{ .Values.application.group | default "other" | quote }}
        url: https://{{ .Release.Name }}-dump978.apps.{{ .Values.cluster.name }}.{{ .Values.cluster.top_level_domain }}
        interval: 60s
        conditions:
{{- if .Values.gatus }}
  {{- if .Values.gatus.responseCode }}
          - "[STATUS] == {{ .Values.gatus.responseCode }}"
  {{- else }}
          - "[STATUS] == 200"
  {{- end }}
{{- else }}
          - "[STATUS] == 200"
{{- end }}
