apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Release.Name }}-ultrafeeder
  labels:
    kasten/backup: "true"
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  revisionHistoryLimit: 1
  podManagementPolicy: OrderedReady
  updateStrategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app.kubernetes.io/controller: ultrafeeder
      app.kubernetes.io/name: {{ .Release.Name }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  serviceName: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
        app.kubernetes.io/controller: ultrafeeder
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/name: {{ .Release.Name }}
    spec:
      enableServiceLinks: false
      serviceAccountName: {{ .Release.Name }}
      automountServiceAccountToken: true
      securityContext:
        fsGroup: 0
        runAsGroup: 0
        runAsUser: 0
      containers:
        - name: ultrafeeder
          env:
          - name: TZ
            value: {{ .Values.cluster.timezone }}
          {{- range $key, $value := .Values.pods.ultrafeeder.env }}
          - name: {{ $key }}
            value: "{{ $value }}"
          {{- end }}
          envFrom:
            - secretRef:
                name: {{ .Release.Name }}
          securityContext:
            privileged: true
          resources:
            requests:
                squat.ai/adsb1090: '1'
            limits:
                squat.ai/adsb1090: '1'
          image: {{ .Values.pods.ultrafeeder.image.repository }}:{{ .Values.pods.ultrafeeder.image.tag }}
          imagePullPolicy: IfNotPresent
