{{- if .Capabilities.APIVersions.Has "route.openshift.io/v1" }}
---
kind: Route
apiVersion: route.openshift.io/v1
metadata:
  annotations:
    route.openshift.io/termination: "edge"
  name: {{ .Release.Name }}-dump978
spec:
  host: "{{ .Release.Name }}-dump978.apps.{{ .Values.cluster.name }}.{{ .Values.cluster.top_level_domain }}"
  path: /
  to:
    kind: Service
    name: {{ .Release.Name }}-dump978
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
{{- else }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - "{{ .Release.Name }}-dump978.apps.{{ .Values.cluster.name }}.{{ .Values.cluster.top_level_domain }}"
      secretName: "{{ .Release.Name }}-tls"
  rules:
    - host: "{{ .Release.Name }}-dump978.apps.{{ .Values.cluster.name }}.{{ .Values.cluster.top_level_domain }}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}-dump978
                port:
                  name: http
{{- end }}
