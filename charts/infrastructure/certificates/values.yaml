cluster:
  top_level_domain: example.com
  name: openshift
  admin_email: admin@example.com
  timezone: America/New_York
  external_top_level_domain: "example.org"
  storage:
    #config:
    #  storageClassName: synology-iscsi
    media:
      nfs:
        server: nas
        path: /volume1/media

certificates:
  letsencrypt:
    issuer: staging
  aws:
    region: us-west-1
