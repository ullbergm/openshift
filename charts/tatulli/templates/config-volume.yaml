---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tatulli-config
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: synology-iscsi
  resources:
    requests:
      storage: 15Gi