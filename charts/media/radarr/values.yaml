cluster:
  top_level_domain: example.com
  name: openshift
  admin_email: admin@example.com
  timezone: America/New_York
  storage:
    #config:
    #  storageClassName: synology-iscsi
    media:
      nfs:
        server: nas
        path: /volume1/media

application:
  name: Radarr
  group: Media
  icon: simple-icons:radarr
  iconColor: ""
  image: "https://raw.githubusercontent.com/Radarr/Radarr/v5.18.0/Logo/256.png"
  description: "Movie management"
  port: 7878
  location: 0

pods:
  main:
    image:
      repository: ghcr.io/home-operations/radarr
      # renovate: datasource=docker depName=ghcr.io/home-operations/radarr versioning=semver
      tag: 5.18.0
      pullPolicy: IfNotPresent
    env:
      RADARR__INSTANCE_NAME: Radarr
      RADARR__PORT: "{{ .Values.application.port }}"
      RADARR__APPLICATION_URL: "https://radarr.{{ .Values.cluster.top_level_domain }}"
      RADARR__LOG_LEVEL: info
