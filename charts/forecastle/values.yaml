forecastle:
  forecastle:
    route:  
      enabled: true
      annotations: {}
      additionalLabels: {}
      host: "www"
      port:
        targetPort: http
      wildcardPolicy: None
      tls:
        termination: edge
        insecureEdgeTerminationPolicy: Redirect
    config:
      namespaceSelector:
        matchNames:
        - default
      title: "Cluster Dashboard"