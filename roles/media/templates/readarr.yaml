{{ if .Values.applications.readarr }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: readarr
  namespace: openshift-gitops
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: readarr
    server: "{{ .Values.spec.destination.server }}"
  project: default
  source:
    path: charts/media/readarr
    repoURL: "{{ .Values.spec.source.repoURL }}"
    targetRevision: "{{ .Values.spec.source.targetRevision }}"
    helm:
      valuesObject:
{{- with .Values }}
  {{. | toYaml | nindent 10 }}
{{- end }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
{{ end }}
